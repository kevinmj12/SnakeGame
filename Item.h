#include <iostream>
#include "Map.h"
#include <random>
using namespace std;

class Item {
private:
    bool isApple;
public:
    Item();
    void generateApple(Map &map) {
        
    }
    void generatePoision(Map &map) {
        
    }
};