#include <iostream>
#include "Map.h"
#include <random>
using namespace std;

class Item {
private:
    bool isApple;
    int countApple;
    int countPoision;
public:
    Item();
    void generateApple(Map &map) {
        
    }
    void generatePoision(Map &map) {
        
    }
};